export interface ChainOption {
  id: string
  name: string
  icon: string
  color: string
  iconColor: string
  symbol: string
}

export interface NavItem {
  id: string
  label: string
  path: string
}
